red line errors "" in:
###levelAssetBinding.test
red line errors "describe" in: describe('Level Asset Binding', () => {
red line errors all of "test" in: levelAssetBinding.test
red line errors all of "expect" in: levelAssetBinding.test

##AssetRegistryTest

red line errors "react", "pixi.js" , "pixi.js" in:
import { useEffect, useRef, useState } from 'react'
import * as PIXI from 'pixi.js'
import { AssetRegistry } from '@/core/AssetRegistry'
red line errors until the end starting from:
return (
    <div style={{ fontFamily: 'monospace, sans-serif', padding: '20px' }}>


##AssetRegistry
red line errors "pixi.js" in: import * as PIXI from 'pixi.js';
red line errors "process" in:     
  // Debug logging in development
      if (process.env.NODE_ENV === 'development') {
red line errors " process" in:  
if (typeof window !== 'undefined' && process.env.NODE_ENV === 'development') {
red line errors "jest" in:  if (typeof jest === 'undefined') {